[tox]
isolated_build = True
envlist = py310,py311

[testenv]
deps =
    black
    coverage
    flake8
    isort
    mccabe
    pylint
    pytest
commands =
    black deepehrgraph
    isort deepehrgraph
    flake8 deepehrgraph --max-complexity 10
    pylint deepehrgraph
    pytest ./tests/
    coverage run --source=deepehrgraph --branch -m pytest .
    coverage report -m
